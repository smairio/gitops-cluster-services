# =============================================================================
# CloudNativePG Cluster - Base Configuration
# =============================================================================
# This is the shared base cluster definition.
# Overlays will patch this for init vs recovery modes.
# =============================================================================
apiVersion: postgresql.cnpg.io/v1
kind: Cluster
metadata:
  name: postgres-cluster
  namespace: postgres
spec:
  description: "Production PostgreSQL Cluster"
  imageName: ghcr.io/cloudnative-pg/postgresql:17.2
  instances: 3

  # PostgreSQL Configuration
  postgresql:
    parameters:
      max_connections: "200"
      shared_buffers: "256MB"
      effective_cache_size: "768MB"
      maintenance_work_mem: "64MB"
      checkpoint_completion_target: "0.9"
      wal_buffers: "16MB"
      default_statistics_target: "100"
      random_page_cost: "1.1"
      effective_io_concurrency: "200"
      work_mem: "4MB"
      huge_pages: "off"
      min_wal_size: "512MB"
      max_wal_size: "1GB"

  # Enable PG_STAT_STATEMENTS for monitoring
  enableSuperuserAccess: true

  # Superuser secret (pulled from Vault via ExternalSecret)
  superuserSecret:
    name: postgres-superuser

  # ==========================================================================
  # MANAGED ROLES - CloudNativePG will manage these user passwords
  # ==========================================================================
  managed:
    roles:
      - name: awx
        ensure: present
        login: true
        createdb: false
        createrole: false
        inherit: true
        passwordSecret:
          name: postgres-awx-owner
      - name: keycloak
        ensure: present
        login: true
        createdb: false
        createrole: false
        inherit: true
        passwordSecret:
          name: postgres-keycloak-owner

  # Storage configuration
  storage:
    size: 10Gi
    storageClass: hcloud-volumes

  # WAL storage (separate volume for better performance)
  walStorage:
    size: 2Gi
    storageClass: hcloud-volumes

  # Resource limits
  resources:
    requests:
      memory: "512Mi"
      cpu: "250m"
    limits:
      memory: "1Gi"
      cpu: "1000m"

  # Affinity - spread across nodes
  affinity:
    enablePodAntiAffinity: true
    topologyKey: kubernetes.io/hostname

  # Monitoring
  monitoring:
    enablePodMonitor: true

  # ==========================================================================
  # BACKUP CONFIGURATION - Using Barman Cloud Plugin v0.10+
  # ==========================================================================
  # The ObjectStore must be in the same namespace as the Cluster.
  # isWALArchiver: true enables both WAL archiving and data directory backups.
  # ==========================================================================
  plugins:
    - name: barman-cloud.cloudnative-pg.io
      isWALArchiver: true
      parameters:
        barmanObjectName: hetzner-s3-store

  # ==========================================================================
  # BOOTSTRAP SECTION - Will be patched by overlays
  # ==========================================================================
  # The bootstrap section is intentionally empty here.
  # - init overlay: adds initdb bootstrap
  # - recovery overlay: adds recovery from backup
  # ==========================================================================
