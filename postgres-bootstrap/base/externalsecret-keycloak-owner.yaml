# =============================================================================
# ExternalSecret - Keycloak Database Owner Credentials from Vault
# =============================================================================
# This secret is used by CloudNativePG to set the password for the 'keycloak' user
# Keycloak application must use the same Vault path to get the matching password
# =============================================================================
apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: postgres-keycloak-owner
  namespace: postgres
  annotations:
    argocd.argoproj.io/sync-wave: "-2"
spec:
  refreshInterval: 1h
  secretStoreRef:
    name: vault-backend
    kind: ClusterSecretStore
  target:
    name: postgres-keycloak-owner
    creationPolicy: Owner
    template:
      type: kubernetes.io/basic-auth
      data:
        username: "keycloak"
        password: "{{ .password }}"
  data:
    - secretKey: password
      remoteRef:
        key: khalil/argocd/keycloak-postgres
        property: password
